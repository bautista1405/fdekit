import type { DockerComposeCommand } from '../types/index.js';

export const defaultDockerComposeCommand: DockerComposeCommand = 'docker compose';

export const defaultDockerComposeFile = 'docker-compose.yml';
